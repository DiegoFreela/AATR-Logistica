# O arquivo .sql descreve a estrutura do banco e traz os acessos
# iniciais. Ninguém precisa baixá-lo pelo navegador.

<IfModule mod_authz_core.c>
    Require all denied
</IfModule>

<IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
</IfModule>
